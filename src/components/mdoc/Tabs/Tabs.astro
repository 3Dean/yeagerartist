---
import TabsComponent from './index.svelte'
import type { TabItem } from './tabs'

interface Props {
    tabs: TabItem[]
}

const { tabs } = Astro.props
---
<TabsComponent tabs={tabs} client:visible/>
